CREATE TABLE `d97ahb9j2s8g2g`.`users` (
  `user_id` INT NULL AUTO_INCREMENT,
  `user_name` TEXT NOT NULL,
  PRIMARY KEY (`user_id`));


  CREATE TABLE `d97ahb9j2s8g2g`.`consumption` (
    `cons_id` INT NULL AUTO_INCREMENT,
    `cons_name` TEXT NOT NULL,
    `cons_calories` INT NOT NULL,
    `date` DATE NULL DEFAULT now(),
    `user_id` INT NOT NULL REFERENCES users (user_id),
    PRIMARY KEY (`cons_id`));
